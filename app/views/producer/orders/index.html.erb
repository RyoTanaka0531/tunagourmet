<h3>受注一覧</h3>
<% @orders.each do |order|%>
<table>
<td>
    <th>品目名</th>
    <th>売価</th>
    <th>発注量</th>
    <th>金額</th>
    <th>メモ</th>
</tr>
<tr>
    <%# <% @product.each do |product|%> %>
        <td><%= @product.name %></td>
        <td><%= @product.price %></td>
    <%# <% end %> %>
    <td><%= order.count %></td>
    <td><%= order.payment %></td>
    <td><%= order.remark %></td>
</tr>
<tr>
	<%# <td><%= link_to order.created_at.to_s(:datetime_jp), producer_order_path(order) %></td>
	<%# <td><%= order.buyer.company_name %></td>
	<%# <td><%= order.price %></td>
	<td><%= order.delivery %></td>
	<%# <td><%= order.products.price %></td>
	<td><%= order.remark %></td>
	<%# <td><%= order.delivery %></td>
	<td><%= order.count %></td>
	<%# <td><% products = Product.where(order_id:order.id) %>
		<%# <% count = 0 %>
		<%# <% @products.each do |product| %>
				<%# <% product.name %>
				<%# <% product.price %>
		<%# <% end %> %>
		<%# <%= count %></td>
	<%# <td><%= order.order_status %></td>
</tr>
</table>
<% end%>